{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "display": "React TS Config",
  "compilerOptions": {
    "jsx": "react-jsx",

    // Don't emit because we use vite as our bundler
    "noEmit": true,
    "declaration": false
  }
}
